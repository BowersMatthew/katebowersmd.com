{
  "name": "kate-bowers-md",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prop-types": "^15.6.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2",
    "react-router-hash-link": "^1.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build:dev": "react-scripts build && aws s3 sync ./build s3://expresstest.com --delete",
    "build:prod": "react-scripts build && aws s3 sync ./build s3://katebowersmd.com --delete",
    "invalidateCache": "aws cloudfront create-invalidation --distribution-id E32OXBR1QCENPI --paths \"/*\"",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
